import 'package:flutter/material.dart';

class AppColors {
  // Primary: Deep Sea Blue
  static const Color deepSeaBlue = Color(0xFF003B5C);
  static const Color deepSeaBlueDark = Color(0xFF002A42);
  static const Color deepSeaBlueLight = Color(0xFF005380);

  // Accent: Lime
  static const Color lime = Color(0xFFC0FF00);
  static const Color limeDim = Color(0xFFA6DB00);

  // Backgrounds
  static const Color backgroundDark = Color(0xFF001F33); // Very dark blue for backgrounds
  static const Color surfaceDark = Color(0xFF002A42); // Slightly lighter for cards

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textOnLime = Color(0xFF003B5C); // Deep blue text on lime buttons
}
