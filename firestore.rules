rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if true; // DEBUG: Open for testing
    }
    match /venues/{venueId} {
      allow read, write: if true; // DEBUG: Open for testing
    }
    match /scans/{scanId} {
      allow read, write: if true; // DEBUG: Open for testing
    }
  }
}
